[build]
  publish = "dist"
  command = "npm install && npm run build"

[dev]
  command = "npm run dev"

[build.environment]
  NODE_VERSION = "18"

[context.production.environment]
  NODE_ENV = "production"
